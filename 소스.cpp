#include<stdio.h>
#include<queue>

using namespace std;
queue <int> q;
int main() {
	int t,n;
	char cmd[10];
	scanf("%d", &t);
	for (int i = 0; i < t; i++) {
		scanf("%s %d",cmd,&n);
		if (!strcmp(cmd, "push")) {

		}
		else if (!strcmp(cmd, "")) {

		}
		else if (!strcmp(cmd, "")) {

		}
		else if (!strcmp(cmd, "")) {

		}
		else if (!strcmp(cmd, "")) {

		}
		else if (!strcmp(cmd, "")) {

		}
	}
}